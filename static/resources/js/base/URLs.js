/**
 * Created by jie on 2016/10/26.
 */
function URLs(param1, param2, param3){
    var debug = true
    var mockURLs = {

    };
    var URLs = {

    };
    if(debug === true){
        return mockURLs;
    } else{
        return URLs;
    }
}